<div id="modalCantidad" class="win-modal-overlay" style="display:none;">
    <div class="win-modal-content" style="width: 350px;">
        <div class="win-modal-header">
            <span>Cantidad a Cambiar</span>
            <button class="win-close-btn" onclick="cancelarModalCant()">&times;</button>
        </div>
        <div class="win-modal-body">
            <div class="product-info-mini">
                <label>Producto:</label>
                <div id="txtProductoModal" class="text-bold"></div>
            </div>
            <hr />
            <div class="win-row">
                <div class="win-field" style="flex:1">
                    <label>Máximo</label>
                    <input type="text" id="maxCant" class="win-input readonly" readonly>
                </div>
                <div class="win-field" style="flex:2">
                    <label>Cant. a Cambiar</label>
                    <input type="number" id="inputCantidadCambio" class="win-input" min="1" autofocus>
                </div>
            </div>
        </div>
        <div class="win-modal-footer">
            <button type="button" class="win-button primary" onclick="confirmarCambio()">Aceptar</button>
            <button type="button" class="win-button" onclick="cancelarModalCant()">Cancelar</button>
        </div>
    </div>
</div>

<style>
    .win-modal-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0,0,0,0.5);
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .win-modal-content {
        background: #fff;
        border: 1px solid #0078d7;
        box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    }

    .win-modal-header {
        background: #0078d7;
        color: #fff;
        padding: 8px 12px;
        display: flex;
        justify-content: space-between;
    }

    .win-modal-body {
        padding: 15px;
    }

    .win-modal-footer {
        padding: 10px;
        background: #f0f0f0;
        text-align: right;
    }

    .text-bold {
        font-weight: bold;
        color: #333;
        margin-top: 5px;
    }

    .win-close-btn {
        background: none;
        border: none;
        color: white;
        font-size: 20px;
        cursor: pointer;
    }
</style>